# Add Gmail label rule to group all W3C notifications

## Implementation Steps

### Task 1: Add Gmail label rule to group all W3C notifications

- [x] Add a rule to label all W3C-related GitHub notifications with a single w3c label. This may require defining a list of keywords or repository patterns (e.g. w3c/* repos like w3c/webai-roadmap) that map to the w3c label. Reference: w3c/webai-roadmap#16 is an example of a notification that should be grouped.
